Removent `0.1.0` was not published. The CI build and Apple notarization passed, but the final relay signature check rejected its malformed inline requirement.

The immutable source tag is retained for traceability. The corrected first release is [v0.1.2](v0.1.2.md).
